// GimmiCoin_Web.ino
// 
// Este archivo contiene SOLO las declaraciones del módulo web.
// Las implementaciones están en GimmiCoin_Main.ino para evitar redefiniciones.
//
// NOTA: En Arduino IDE, todos los archivos .ino en la misma carpeta se compilan juntos
// como un solo sketch, por lo que las implementaciones deben estar solo en un archivo.

// Declaraciones de funciones del módulo web
// (Las implementaciones están en GimmiCoin_Main.ino)

// void startWebServer(const char* ssid, const char* password);
// void handleRoot();
// void handleGetStatus();
// void handlePostReset();
// void handleNotFound();

// Fin del archivo de declaraciones